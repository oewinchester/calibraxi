# Issue tracker

Issues for this repository live in GitHub Issues at `oewinchester/calibraxi`.

Use the explicit repository selector for issue operations because the current local Git remote has not been changed:

```text
gh --repo oewinchester/calibraxi issue <command>
```

Pull requests are not a separate triage request surface. This file records routing only; project requirements and architecture remain in the canonical CalibraXI documents.
